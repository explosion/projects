title: "Weights & Biases logging integration"
description: "Use [Weights & Biases](https://www.wandb.com/) for logging of training experiments."

# Variables can be referenced across the project.yml using ${vars.var_name}
vars:
  default_config: "default_config.cfg"

# These are the directories that the project needs. The project CLI will make
# sure that they always exist.
directories: ["scripts", "assets", "results", "configs"]

# Assets that should be downloaded or available in the directory. You can replace
# this with your own input data.
assets:
  - dest: "assets/aclImdb_v1.tar.gz"
    url: "http://ai.stanford.edu/~amaas/data/sentiment/aclImdb_v1.tar.gz"
    description: "Movie Review Dataset for sentiment analysis by Maas et al., ACL 2011."

workflows:
  all:
    - data
    - configs
    - train

# Project commands, specified in a style similar to CI config files (e.g. Azure
# pipelines). The name is the command name that lets you trigger the command
# via "spacy project run [command] [path]". The help message is optional and
# shown when executing "spacy project run [optional command] [path] --help".
commands:
  - name: "install"
    help: "Install dependencies and login to Weights & Biases"
    script:
      - "pip install -r requirements.txt"
      - "wandb login"
    deps:
      - "requirements.txt"

  - name: "data"
    help: "Extract the gold-standard annotations."
    script:
      - "tar -xzf ./assets/aclImdb_v1.tar.gz -C ./assets/"
    deps:
      - "assets/aclImdb_v1.tar.gz"
    outputs:
      - "assets/aclImdb/train/"
      - "assets/aclImdb/test/"

  - name: "configs"
    help: "Create variations on an initial, default config file for IMDB sentiment classification."
    script:
      - "python -m spacy init config -p textcat -l en ./configs/${vars.default_config} --cpu"
      - "python ./scripts/customize_configs.py configs ${vars.default_config}"

  - name: "train"
    help: "Run customized training runs."
    script:
      - "python ./scripts/run_configs.py configs ${vars.default_config} results"
    deps:
      - "assets/aclImdb/train/"
      - "assets/aclImdb/test/"
      - "configs/"
